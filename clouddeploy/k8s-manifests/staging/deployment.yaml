apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-staging
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app-staging
  template:
    metadata:
      labels:
        app: app-staging
    spec:
      containers:
        - name: app
          image: us-central1-docker.pkg.dev/$PROJECT_ID/my-repo/app:$TAG
          ports:
            - containerPort: 8080